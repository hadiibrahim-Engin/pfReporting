<section>
    <h2>Statistik-Übersicht</h2>
    <div class="stat-grid">

        {% macro stacked(title, counts) %}
        {% set total = counts.ok + counts.warning + counts.violation %}
        {% if total > 0 %}
        <div class="stat-item">
            <div class="stat-title">{{ title }}</div>
            <div class="stacked-bar">
                {% if counts.ok > 0 %}
                <div class="sb-ok" style="width:{{ (counts.ok / total * 100) | round(2) }}%">{{ counts.ok }}</div>
                {% endif %}
                {% if counts.warning > 0 %}
                <div class="sb-warn" style="width:{{ (counts.warning / total * 100) | round(2) }}%">{{ counts.warning }}</div>
                {% endif %}
                {% if counts.violation > 0 %}
                <div class="sb-verl" style="width:{{ (counts.violation / total * 100) | round(2) }}%">{{ counts.violation }}</div>
                {% endif %}
            </div>
        </div>
        {% endif %}
        {% endmacro %}

        {{ stacked('Spannungsband', overall.counts.voltage) }}
        {{ stacked('Thermische Auslastung', overall.counts.thermal) }}
        {{ stacked('(N-1)-Sicherheit', overall.counts.n1) }}

    </div>
    <div class="legende">
        <span class="leg-ok">&#9632; OK</span>
        <span class="leg-warn">&#9632; Warnung</span>
        <span class="leg-verl">&#9632; Verletzung</span>
    </div>
</section>
